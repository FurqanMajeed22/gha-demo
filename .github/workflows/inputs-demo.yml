name: Deploy App
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Deployment environment"
        required: true
        default: "dev"
jobs:
 input-demo:
   runs-on: ubuntu-latest
   steps:
     - name: Deploy the app
       run: echo "Deployin the app to ${{inputs.environment}}"

     
     
