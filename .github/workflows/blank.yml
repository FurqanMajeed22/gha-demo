name: Hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Stage
        run: echo "A demo build"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test-phase
        run: echo "Running tests"
  depoy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the App
        run: echo "Deployin the app"
      
        
        
        
