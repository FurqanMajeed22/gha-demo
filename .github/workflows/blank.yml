name: Hello world
on: 
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Stage
        run: echo "A demo build"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test-phase
        run: echo "Running tests"
  depoy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the App
        run: echo "Deploying the app"
      
        
        
        
